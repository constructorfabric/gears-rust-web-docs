name: CI

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - 'src/**'
      - 'scripts/**'
      - 'config/**'
      - 'public/**'
      - 'astro.config.mjs'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'lunaria.config.json'
      - '.github/workflows/ci.yml'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build:
    name: Lint, build & link-check
    runs-on: ubuntu-latest
    env:
      GEARS_RUST_PATH: tmp/gears-rust
      CARGO_GEARS_PATH: tmp/cargo-gears
      BASE: /gears-rust-web-docs
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          # Lunaria computes per-file translation freshness from git history,
          # so the build needs the full history (not a shallow clone). The
          # /i18n dashboard is emitted during `pnpm build` below.
          fetch-depth: 0

      - name: Checkout gears-rust (docs source)
        uses: actions/checkout@v7
        with:
          repository: constructorfabric/gears-rust
          ref: main
          path: tmp/gears-rust

      - name: Checkout cargo-gears (CLI docs source)
        uses: actions/checkout@v7
        with:
          repository: constructorfabric/cargo-gears
          ref: main
          path: tmp/cargo-gears

      - name: Set up pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Sync docs from all sources
        run: pnpm sync

      - name: Type & content check
        run: pnpm check

      - name: Markdown lint
        run: pnpm lint:md

      - name: Build
        run: pnpm build

      - name: Link check
        run: pnpm links
